const WritingPanel = () => {
    return ( 
        <div className="notebook">
            <p>All the writing stuff will be on the right. It should cover the screen, work on the selection panel for now.</p>
        </div>
     );
}
 
export default WritingPanel;